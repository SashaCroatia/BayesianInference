% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PR-HMC-norm.R
\name{hmc_norm}
\alias{hmc_norm}
\title{Hamiltonian Monte Carlo for Logistic Regression with Normal or Data-Driven Priors
Fits a binary logistic regression model using Hamiltonian Monte Carlo (HMC) via the \pkg{nimble} and \pkg{nimbleHMC} packages, with normal priors based on previous coefficient samples.}
\usage{
hmc_norm(
  X,
  y,
  niter = 5000,
  nburnin = 1000,
  nchains = 1,
  inits = NULL,
  KDE = FALSE,
  C = NA
)
}
\arguments{
\item{X}{A numeric matrix of predictors (n × M), where \code{n} is the number of observations and \code{M} is the number of predictors.}

\item{y}{A numeric or integer vector of binary responses (0/1), length \code{n}.}

\item{niter}{Integer, total number of MCMC iterations to run. Default is 5000.}

\item{nburnin}{Integer, number of burn-in iterations to discard. Default is 1000.}

\item{nchains}{Integer, number of chains to run. Default is 1.}

\item{inits}{Optional numeric vector of initial values for \code{beta}. If \code{NULL}, defaults to a zero vector.}

\item{KDE}{Logical, whether to use data-driven priors based on a set of coefficient samples.
If \code{FALSE} (default), priors are \eqn{N(0, 100^2)}.
If \code{TRUE}, priors are \eqn{N(\mathrm{mean}(C_j), \mathrm{sd}(C_j)^2)} for each coefficient \eqn{j}.}

\item{C}{A numeric matrix of coefficient samples (n × M) used only if \code{KDE = TRUE}.}
}
\value{
A list containing posterior samples
}
\description{
Hamiltonian Monte Carlo for Logistic Regression with Normal or Data-Driven Priors
Fits a binary logistic regression model using Hamiltonian Monte Carlo (HMC) via the \pkg{nimble} and \pkg{nimbleHMC} packages, with normal priors based on previous coefficient samples.
}
